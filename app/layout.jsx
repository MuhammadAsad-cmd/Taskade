import Sidebar from "./components/Sidebar/Sidebar";
import "./globals.css";

export const metadata = {
  title: "X-TWO-AI",
  description: "Generated by create next app",
  links: [
    {
      rel: "stylesheet",
      href: "https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap",
    },
  ],
};

export default function RootLayout({ children }) {
  return (
    <html lang="en" className="h-full">
      <body className="h-full overflow-hidden">
        <div className="flex h-full w-full">
          {/* Sidebar */}
          <div className="hidden h-full w-full max-w-[300px] lg:block">
            <Sidebar />
          </div>

          {/* Main content with a scrollable area and custom scrollbar */}
          <div className="h-full w-full p-2">
            <div className="custom-scrollbar h-full w-full overflow-y-auto rounded-xl border bg-white pb-2">
              {children}
            </div>
          </div>
        </div>
      </body>
    </html>
  );
}
